# Talk2PDF
Most "Chat with your PDF" apps are actually pretty frustrating. They cut your document into random blocks based on character counts, which means the AI often loses the thread of a sentence right when it’s getting to the good part. I built this version to be smarter. Instead of blindly cutting text, it uses Semantic Chunking. 
